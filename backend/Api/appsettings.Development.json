{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },

  // Local development only. These are throwaway container credentials created by
  // docker-compose.yml on this machine — not a secret, and not a credential that
  // exists anywhere else. A real connection string is never committed
  // (LoanTracker_Stack_Rules.md: use `dotnet user-secrets` for anything real).
  //
  // The database name is RUN-SCOPED rather than a fixed shared name, per
  // CLAUDE.md Incremental Session Patterns. EF Core's Migrate() creates it on
  // first run, so no create-if-absent step is needed.
  "ConnectionStrings": {
    "LoanTracker": "Host=localhost;Port=5433;Database=loantracker_dev_20260919_pilot;Username=loantracker;Password=loantracker_dev"
  }
}
